{
  "name": "lmplus",
  "private": true,
  "version": "2.5.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "deploy:backend:preview": "bash scripts/deploy-backend-preview.sh",
    "sync:backend:secrets": "bash scripts/sync-backend-secrets.sh",
    "release:beta": "bash scripts/publish-beta-release.sh",
    "release:beta:signed": "bash scripts/publish-signed-beta-release.sh"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "typescript": "~5.7.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-shell": "^2",
    "@tauri-apps/plugin-updater": "^2",
    "@tauri-apps/plugin-process": "^2"
  }
}
